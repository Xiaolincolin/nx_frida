{
  "name": "temp-agent",
  "version": "1.0.0",
  "description": "A description of the project briefing",
  "private": true,
  "main": "agent.ts",
  "scripts": {
    "build": "frida-compile agent.ts -c -o ./agent.js",
    "watch": "frida-compile agent.ts -w -o ./agent.js"
  },
  "devDependencies": {
    "@types/frida-gum": "^18.0.0",
    "@types/node": "^17.0.36",
    "frida-compile": "^11.1.0",
    "frida-remote-stream": "^6.0.1"
  }
}
